import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import { ifDefined } from "lit-html/directives/if-defined";

export const Template = (args = {}) => {
  const { kind, label, disabled, _children = "" } = args;
  return html`<ui-button
    kind=${ifDefined(kind)}
    label=${ifDefined(label)}
    ?disabled=${disabled}
    >${html([_children])}</ui-button
  >`;
};

<Meta
  title="Atoms/Button"
  component={`ui-button`}
  argTypes={{
    disabled: {
      control: "boolean",
    },
    kind: {
      control: {
        type: "radio",
        options: ["default", "primary", "secondary"],
      },
    },
    label: {
      defaultValue: "Label Text",
      control: "text",
    },
    _children: {
      defaultValue: "",
      control: "text",
    },
  }}
/>

# Button

The basic button atom.

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      _children: "Button Label",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Primary

<Canvas>
  <Story
    name="Primary"
    args={{
      kind: "primary",
      _children: "Button Label",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      disabled: true,
      _children: "Button Label",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Interface

<ArgsTable />
